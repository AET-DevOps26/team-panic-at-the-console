# Encrypt with: sops --encrypt infra/helm/secrets/values.monitoring.dec.yaml > infra/helm/secrets/values.monitoring.enc.yaml
# Commit only values.monitoring.enc.yaml — the .dec.yaml is gitignored.
kube-prometheus-stack:
  grafana:
    adminPassword: "CHANGE_ME"
