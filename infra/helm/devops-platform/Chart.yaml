apiVersion: v2
name: devops-platform
description: Helm chart for DevOps Project 2026 services
type: application
version: 0.1.0
appVersion: "0.1.0"
# No chart dependencies: observability uses namespace-scoped Prometheus/Grafana
# resources so it also works on Azure without cluster-scoped RBAC or CRDs.
