apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    # Prometheus serves its API under the route-prefix from --web.external-url.
    url: http://prometheus:9090/prometheus
    isDefault: true
    editable: false
