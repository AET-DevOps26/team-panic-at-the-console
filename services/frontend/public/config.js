// Deployment-specific runtime config, loaded before the app bundle (see index.html).
// In the Docker image this file is regenerated from config.js.template at container
// start with GRAFANA_URL / PROMETHEUS_URL substituted. Values are absolute URLs or
// ":port" (resolved against the host serving the page); empty hides the link.
window.__APP_CONFIG__ = {
  grafanaUrl: "",
  prometheusUrl: "",
};
