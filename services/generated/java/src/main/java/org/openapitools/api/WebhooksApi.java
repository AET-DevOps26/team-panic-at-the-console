/**
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech) (7.14.0).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package org.openapitools.api;

import org.openapitools.model.ErrorResponse;
import java.util.Map;
import org.openapitools.model.WebhookReceipt;
import io.swagger.v3.oas.annotations.ExternalDocumentation;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.Parameter;
import io.swagger.v3.oas.annotations.Parameters;
import io.swagger.v3.oas.annotations.media.ArraySchema;
import io.swagger.v3.oas.annotations.media.Content;
import io.swagger.v3.oas.annotations.media.Schema;
import io.swagger.v3.oas.annotations.responses.ApiResponse;
import io.swagger.v3.oas.annotations.security.SecurityRequirement;
import io.swagger.v3.oas.annotations.tags.Tag;
import io.swagger.v3.oas.annotations.enums.ParameterIn;
import io.swagger.v3.oas.annotations.media.ExampleObject;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.lang.Nullable;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.context.request.NativeWebRequest;
import org.springframework.web.multipart.MultipartFile;

import jakarta.validation.Valid;
import jakarta.validation.constraints.*;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import jakarta.annotation.Generated;

@Generated(value = "org.openapitools.codegen.languages.SpringCodegen", comments = "Generator version: 7.14.0")
@Validated
@Tag(name = "webhooks", description = "Machine-facing webhook ingest (webhook-service). Served at `/webhooks` on the host root by the ingress / compose edge — NOT under `/api/v1` (see the path-level `servers` override). Senders authenticate per source with an HMAC signature over the raw body instead of a session. ")
public interface WebhooksApi {

    default Optional<NativeWebRequest> getRequest() {
        return Optional.empty();
    }

    /**
     * POST /webhooks/{source} : Ingest a webhook from an external system
     * Persists the payload verbatim as an External Event (ADR 0008) and publishes &#x60;external.event.received&#x60; to NATS for the rule engine. Sources with a configured secret must sign the raw request body (&#x60;X-Hub-Signature-256&#x60;, GitHub convention); with &#x60;WEBHOOK_REQUIRE_SIGNATURE&#x3D;true&#x60; sources without a secret are rejected. Redeliveries carrying an already-seen delivery id are acknowledged with the original event id (&#x60;duplicate: true&#x60;) and not re-published.
     *
     * @param source Operator-chosen slug identifying the sending system. (required)
     * @param requestBody Raw webhook payload; persisted verbatim. (required)
     * @param xHubSignature256 &#x60;sha256&#x3D;&lt;hex HMAC-SHA256 of the raw body&gt;&#x60;, keyed with the secret configured for the source. Required when the source has a secret. (optional)
     * @param xGitHubEvent GitHub event name; used to derive the normalised eventType. (optional)
     * @param xGitHubDelivery GitHub delivery id; used for redelivery dedup. (optional)
     * @param xEventType Explicit normalised event type for non-GitHub senders. (optional)
     * @param xDeliveryId Delivery id for non-GitHub senders; used for redelivery dedup. (optional)
     * @return Event ingested (or redelivery acknowledged) (status code 202)
     *         or Invalid source slug or non-object JSON body (status code 400)
     *         or Missing or invalid signature (status code 401)
     */
    @Operation(
        operationId = "receiveWebhook",
        summary = "Ingest a webhook from an external system",
        description = "Persists the payload verbatim as an External Event (ADR 0008) and publishes `external.event.received` to NATS for the rule engine. Sources with a configured secret must sign the raw request body (`X-Hub-Signature-256`, GitHub convention); with `WEBHOOK_REQUIRE_SIGNATURE=true` sources without a secret are rejected. Redeliveries carrying an already-seen delivery id are acknowledged with the original event id (`duplicate: true`) and not re-published.",
        tags = { "webhooks" },
        responses = {
            @ApiResponse(responseCode = "202", description = "Event ingested (or redelivery acknowledged)", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = WebhookReceipt.class))
            }),
            @ApiResponse(responseCode = "400", description = "Invalid source slug or non-object JSON body", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = ErrorResponse.class))
            }),
            @ApiResponse(responseCode = "401", description = "Missing or invalid signature", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = ErrorResponse.class))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.POST,
        value = "/webhooks/{source}",
        produces = { "application/json" },
        consumes = { "application/json" }
    )

    default ResponseEntity<WebhookReceipt> receiveWebhook(
        @Pattern(regexp = "^[a-z0-9][a-z0-9_-]{0,63}$") @Parameter(name = "source", description = "Operator-chosen slug identifying the sending system.", required = true, in = ParameterIn.PATH) @PathVariable("source") String source,
        @Parameter(name = "request_body", description = "Raw webhook payload; persisted verbatim.", required = true) @Valid @RequestBody Map<String, Object> requestBody,
        @Parameter(name = "X-Hub-Signature-256", description = "`sha256=<hex HMAC-SHA256 of the raw body>`, keyed with the secret configured for the source. Required when the source has a secret.", in = ParameterIn.HEADER) @RequestHeader(value = "X-Hub-Signature-256", required = false) @Nullable String xHubSignature256,
        @Parameter(name = "X-GitHub-Event", description = "GitHub event name; used to derive the normalised eventType.", in = ParameterIn.HEADER) @RequestHeader(value = "X-GitHub-Event", required = false) @Nullable String xGitHubEvent,
        @Parameter(name = "X-GitHub-Delivery", description = "GitHub delivery id; used for redelivery dedup.", in = ParameterIn.HEADER) @RequestHeader(value = "X-GitHub-Delivery", required = false) @Nullable String xGitHubDelivery,
        @Parameter(name = "X-Event-Type", description = "Explicit normalised event type for non-GitHub senders.", in = ParameterIn.HEADER) @RequestHeader(value = "X-Event-Type", required = false) @Nullable String xEventType,
        @Parameter(name = "X-Delivery-Id", description = "Delivery id for non-GitHub senders; used for redelivery dedup.", in = ParameterIn.HEADER) @RequestHeader(value = "X-Delivery-Id", required = false) @Nullable String xDeliveryId
    ) {
        getRequest().ifPresent(request -> {
            for (MediaType mediaType: MediaType.parseMediaTypes(request.getHeader("Accept"))) {
                if (mediaType.isCompatibleWith(MediaType.valueOf("application/json"))) {
                    String exampleString = "{ \"id\" : \"018e2c5f-1234-7abc-8def-0000000000e1\", \"source\" : \"github\", \"eventType\" : \"ci_failure\", \"duplicate\" : false, \"receivedAt\" : \"2000-01-23T04:56:07.000+00:00\" }";
                    ApiUtil.setExampleResponse(request, "application/json", exampleString);
                    break;
                }
                if (mediaType.isCompatibleWith(MediaType.valueOf("application/json"))) {
                    String exampleString = "{ \"message\" : \"Invalid email or password\" }";
                    ApiUtil.setExampleResponse(request, "application/json", exampleString);
                    break;
                }
                if (mediaType.isCompatibleWith(MediaType.valueOf("application/json"))) {
                    String exampleString = "{ \"message\" : \"Invalid email or password\" }";
                    ApiUtil.setExampleResponse(request, "application/json", exampleString);
                    break;
                }
            }
        });
        return new ResponseEntity<>(HttpStatus.NOT_IMPLEMENTED);

    }

}
