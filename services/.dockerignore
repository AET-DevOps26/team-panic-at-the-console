# Default-deny: shared services/ context builds only include listed trees.
# See container-ci.yml matrix `include` entries and per-service Dockerfiles.
*
!gateway/
!incident-service/
!genai-service/
!generated/

# Within the allowed dirs, prune dev artifacts
**/target/
**/.pixi/
**/node_modules/
**/.git
**/.gitignore
