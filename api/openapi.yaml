openapi: 3.1.0

info:
  title: DevOps Incident Platform API
  version: 0.0.1
  description: REST API for the DevOps incident management platform.
  license:
    name: MIT
    url: https://opensource.org/license/mit

tags:
  - name: health
    description: Service health and readiness.
  - name: genai
    description: AI-assisted incident analysis (on-demand regeneration triggers).
  - name: incidents
    description: Incident read APIs and internal write-back endpoints used by genai-service.

servers:
  - url: http://localhost:4010
    description: Local mock server (pixi run mock-api)
  - url: /api/v1
    description: API gateway (relative, in-cluster)

paths:
  /health:
    get:
      operationId: healthCheck
      summary: Health check
      tags: [health]
      security: []
      responses:
        "200":
          description: Gateway health status
          content:
            application/json:
              schema:
                type: object
                required: [status]
                additionalProperties: false
                properties:
                  status:
                    type: string
                    example: ok
        "503":
          description: Service unavailable
        "400":
          description: Bad request

  /genai/health:
    get:
      operationId: genaiHealth
      summary: GenAI service health check
      tags: [genai]
      security: []
      responses:
        "200":
          description: GenAI service and Ollama reachability status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenaiHealthResponse"
        "503":
          description: Ollama unreachable

  /incidents/{incidentId}/genai/summary:
    post:
      operationId: regenerateSummary
      summary: Trigger summary regeneration for an incident
      tags: [genai]
      parameters:
        - $ref: "#/components/parameters/IncidentIdParam"
      responses:
        "202":
          $ref: "#/components/responses/RegenAccepted"
        "404":
          $ref: "#/components/responses/IncidentNotFound"

  /incidents/{incidentId}/genai/severity:
    post:
      operationId: regenerateSeverity
      summary: Trigger severity suggestion regeneration for an incident
      tags: [genai]
      parameters:
        - $ref: "#/components/parameters/IncidentIdParam"
      responses:
        "202":
          $ref: "#/components/responses/RegenAccepted"
        "404":
          $ref: "#/components/responses/IncidentNotFound"

  /incidents/{incidentId}/genai/solutions:
    post:
      operationId: regenerateSolutions
      summary: Trigger solution suggestions regeneration for an incident
      tags: [genai]
      parameters:
        - $ref: "#/components/parameters/IncidentIdParam"
      responses:
        "202":
          $ref: "#/components/responses/RegenAccepted"
        "404":
          $ref: "#/components/responses/IncidentNotFound"

  /incidents/{incidentId}/genai/postmortem:
    post:
      operationId: regeneratePostmortem
      summary: Trigger postmortem regeneration for a resolved incident
      tags: [genai]
      parameters:
        - $ref: "#/components/parameters/IncidentIdParam"
      responses:
        "202":
          $ref: "#/components/responses/RegenAccepted"
        "404":
          $ref: "#/components/responses/IncidentNotFound"
        "409":
          $ref: "#/components/responses/IncidentNotResolved"

  /incidents/{incidentId}:
    get:
      operationId: getIncident
      summary: Fetch a single incident
      tags: [incidents]
      parameters:
        - $ref: "#/components/parameters/IncidentIdParam"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Incident"
        "404":
          description: Incident not found

  /incidents/{incidentId}/events:
    get:
      operationId: listIncidentEvents
      summary: List Events from the Event Log for one incident, in chronological order
      tags: [incidents]
      parameters:
        - $ref: "#/components/parameters/IncidentIdParam"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/IncidentEvent"
        "404":
          description: Incident not found

  /incidents/{incidentId}/genai/summary/result:
    patch:
      operationId: writeIncidentSummary
      summary: (internal) Write AI-generated Summary back to incident-service
      description: |
        Called by genai-service after an Ollama generation completes.
        Cluster-internal only; not exposed via the gateway.
      tags: [incidents]
      parameters:
        - $ref: "#/components/parameters/IncidentIdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SummaryPatch"
      responses:
        "204":
          description: Saved
        "404":
          description: Incident not found

  /incidents/{incidentId}/genai/severity/result:
    patch:
      operationId: writeIncidentSeveritySuggestion
      summary: (internal) Write AI-suggested Severity back to incident-service
      tags: [incidents]
      parameters:
        - $ref: "#/components/parameters/IncidentIdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SeverityPatch"
      responses:
        "204":
          description: Saved
        "404":
          description: Incident not found

  /incidents/{incidentId}/genai/solutions/result:
    patch:
      operationId: writeIncidentSolutions
      summary: (internal) Write AI-generated Solution Suggestions back to incident-service
      tags: [incidents]
      parameters:
        - $ref: "#/components/parameters/IncidentIdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SolutionsPatch"
      responses:
        "204":
          description: Saved
        "404":
          description: Incident not found

  /incidents/{incidentId}/genai/postmortem/result:
    patch:
      operationId: writeIncidentPostmortem
      summary: (internal) Write AI-generated Postmortem back to incident-service
      tags: [incidents]
      parameters:
        - $ref: "#/components/parameters/IncidentIdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PostmortemPatch"
      responses:
        "204":
          description: Saved
        "404":
          description: Incident not found
        "409":
          description: Incident not in resolved state

components:
  parameters:
    IncidentIdParam:
      name: incidentId
      in: path
      required: true
      description: UUID of the target incident.
      schema:
        type: string
        format: uuid
        example: 018e2c5f-1234-7abc-8def-000000000001

  responses:
    RegenAccepted:
      description: Regeneration task accepted for async processing
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/RegenAccepted"
    IncidentNotFound:
      description: No incident exists with the given ID
    IncidentNotResolved:
      description: Postmortem operations require the incident to be in resolved state

  schemas:
    GenaiHealthResponse:
      type: object
      description: GenAI service health status including Ollama reachability.
      required: [status, ollamaReachable, model]
      additionalProperties: false
      properties:
        status:
          type: string
          example: ok
        ollamaReachable:
          type: boolean
          example: true
        model:
          type: string
          example: qwen2.5:3b

    RegenAccepted:
      type: object
      description: Confirmation that an AI generation task was accepted for async processing.
      required: [accepted, task]
      additionalProperties: false
      properties:
        accepted:
          type: boolean
          example: true
        task:
          type: string
          example: SUMMARY
          enum: [SUMMARY, SEVERITY_SUGGESTION, SOLUTION_SUGGESTIONS, POSTMORTEM]

    Severity:
      type: string
      enum: [SEV1, SEV2, SEV3, SEV4]
      description: Incident severity. SEV1 is highest impact.

    IncidentStatus:
      type: string
      enum: [open, investigating, resolved]

    Incident:
      type: object
      description: An incident as stored by incident-service.
      required: [id, title, status, severity, createdAt]
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
          example: Checkout 5xx spike
        description:
          type: [string, "null"]
        status:
          $ref: "#/components/schemas/IncidentStatus"
        severity:
          $ref: "#/components/schemas/Severity"
        createdAt:
          type: string
          format: date-time
        resolvedAt:
          type: [string, "null"]
          format: date-time

    IncidentEvent:
      type: object
      description: One entry from an incident's append-only Event Log.
      required: [timestamp, type, description]
      additionalProperties: false
      properties:
        timestamp:
          type: string
          format: date-time
        type:
          type: string
          example: status_changed
        description:
          type: string
          example: "status: open -> investigating"

    SummaryResponse:
      type: object
      description: >
        Latest AI-generated narrative summary for an incident (returned by GET
        in a later release; used now as the structured LLM output contract).
      required: [summary]
      additionalProperties: false
      example:
        summary: >-
          Checkout API latency spiked after deploy v2.4.1; error rate on
          payment-service rose to 12%.
      properties:
        summary:
          type: string
          example: >-
            Checkout API latency spiked after deploy v2.4.1; error rate on
            payment-service rose to 12%.

    SeverityResponse:
      type: object
      description: >
        Latest AI-generated severity recommendation with rationale (GET in a
        later release; structured LLM output contract today).
      required: [severity, reason]
      additionalProperties: false
      example:
        severity: SEV2
        reason: >-
          Customer-facing checkout degraded for more than 15 minutes with no
          workaround.
      properties:
        severity:
          type: string
          enum: [SEV1, SEV2, SEV3, SEV4]
          example: SEV2
        reason:
          type: string
          example: >-
            Customer-facing checkout degraded for more than 15 minutes with no
            workaround.

    SolutionsResponse:
      type: object
      description: >
        Latest AI-generated remediation suggestions (GET in a later release;
        structured LLM output contract today).
      required: [solutions]
      additionalProperties: false
      example:
        solutions:
          - Roll back payment-service to v2.3.9
          - Scale checkout-api replicas to 6
      properties:
        solutions:
          type: array
          items:
            type: string
          example:
            - Roll back payment-service to v2.3.9
            - Scale checkout-api replicas to 6

    PostmortemResponse:
      type: object
      description: >
        Latest AI-generated postmortem draft for a resolved incident (GET in a
        later release; structured LLM output contract today).
      required: [rootCause, timeline, actionItems]
      additionalProperties: false
      example:
        rootCause: Connection pool misconfiguration in payment-service v2.4.1
        timeline:
          - "14:02 Deploy v2.4.1 completed"
          - "14:18 Checkout error rate crossed 5%"
        actionItems:
          - Add pool-size validation to deploy checklist
          - Alert on checkout latency SLO burn
      properties:
        rootCause:
          type: string
          example: Connection pool misconfiguration in payment-service v2.4.1
        timeline:
          type: array
          items:
            type: string
          example:
            - "14:02 Deploy v2.4.1 completed"
            - "14:18 Checkout error rate crossed 5%"
        actionItems:
          type: array
          items:
            type: string
          example:
            - Add pool-size validation to deploy checklist
            - Alert on checkout latency SLO burn

    SummaryPatch:
      type: object
      required: [summary]
      additionalProperties: false
      properties:
        summary:
          type: string
          minLength: 1

    SeverityPatch:
      type: object
      required: [severity, reason]
      additionalProperties: false
      properties:
        severity:
          $ref: "#/components/schemas/Severity"
        reason:
          type: string
          minLength: 1

    SolutionsPatch:
      type: object
      required: [solutions]
      additionalProperties: false
      properties:
        solutions:
          type: array
          minItems: 1
          items:
            type: string
            minLength: 1

    PostmortemPatch:
      type: object
      required: [rootCause, timeline, actionItems]
      additionalProperties: false
      properties:
        rootCause:
          type: string
          minLength: 1
        timeline:
          type: array
          minItems: 1
          items:
            type: string
        actionItems:
          type: array
          minItems: 1
          items:
            type: string
