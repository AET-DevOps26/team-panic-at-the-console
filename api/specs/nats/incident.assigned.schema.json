{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "https://github.com/aet-devops26/team-panic-at-the-console/api/specs/nats/incident.assigned.schema.json",
  "description": "Published once per newly assigned user. Carries userId so notification-service can target the assigned user without fetching the full incident, and actorId (the user who changed the assignment) so notification-service can suppress self-assignment notifications.",
  "type": "object",
  "required": ["incidentId", "userId", "timestamp"],
  "additionalProperties": false,
  "properties": {
    "incidentId": { "type": "string", "format": "uuid" },
    "userId": { "type": "string", "format": "uuid" },
    "actorId": { "type": "string", "format": "uuid" },
    "timestamp": { "type": "string", "format": "date-time" }
  }
}
