{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "https://github.com/aet-devops26/team-panic-at-the-console/api/specs/nats/incident.comment.added.schema.json",
  "description": "Carries commentId so subscribers can fetch the specific comment without scanning all comments on the incident, and content so event-service can render the comment in the timeline (comments are immutable, so the copy never goes stale).",
  "type": "object",
  "required": ["incidentId", "commentId", "content", "timestamp"],
  "additionalProperties": false,
  "properties": {
    "incidentId": { "type": "string", "format": "uuid" },
    "commentId": { "type": "string", "format": "uuid" },
    "content": { "type": "string" },
    "timestamp": { "type": "string", "format": "date-time" }
  }
}
